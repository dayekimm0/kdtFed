1. 변수

2. 자료형

3. 형 변환

4. 연산자

5. 조건문
if문
if ~ else문
else if문
switch문
삼항조건 연산자

//break문 // continue문

6. 반복문
> literable한 객체 = 배열
for문
forEach문
for ~ of문
for ~ in문 : 객체 전용

7. 함수 Function = 기능 : 프로그래밍 언어의 꽃
> 어떤 기능을 구현하고 싶을 때 사용

선언 & 호출

매개변수
전역변수
지역변수
스코프변수
블록변수
참조변수

자바스크립트 언어 => 함수
1. 변수에 함수를 담을 수 있음 : 원래는 전통적으로 함수 선언식으로만 함수를 호출했었는데,
익명함수가 등장하면서 함수를 보다 편리하게 작성할 수 있게 됨.
2. 다른 함수의 매개변수로 등장할 수 있음
: callback함수라는 스타일 형식을 창조해낼 수 있게 됨.
3. 다른 함수의 반환값(*return)으로 함수가 등장할 수 있음
